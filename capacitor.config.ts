import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'registro.app',
  appName: 'registroapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
